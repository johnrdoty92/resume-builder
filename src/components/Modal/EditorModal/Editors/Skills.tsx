import { useId } from "react";

export const Skills = ({ skills }: { skills: string[] }) => {
    // TODO: create a component similar to bullet points
    // but it doesn't need an id, just needs to group things
    // together under "skills"
};
